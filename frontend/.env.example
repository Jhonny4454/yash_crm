# Base URL of the Flask REST API.
# Leave the default in development - vite.config.js proxies /api to :5000.
VITE_API_URL=/api/v1

# In production point it at your deployed backend, e.g.
# VITE_API_URL=https://crm.yashinternetservices.in/api/v1

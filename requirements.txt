# ---- Core web stack ----
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.43
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.2
Werkzeug==3.0.3
Jinja2==3.1.4
setuptools>=80

# ---- Database ----
PyMySQL==1.1.1
cryptography==42.0.8

# ---- Config / validation ----
python-dotenv==1.0.1
email-validator==2.1.1

# ---- Background jobs ----
APScheduler==3.10.4

# ---- HTTP client (WhatsApp/Cashfree/Razorpay gateways) ----
requests==2.32.3

# ---- REST API auth (React admin panel + React Native app) ----
PyJWT==2.8.0

# ---- Invoice PDF generation ----
# Pure Python, unlike WeasyPrint which needs GTK/Cairo on Windows.
reportlab==4.2.5

# ---- Cloud file storage (uploads + database backups) ----
# S3-compatible, so the same client works against Cloudflare R2, Backblaze B2,
# Wasabi, Amazon S3 or Supabase Storage - only the endpoint URL differs.
# Optional at runtime: with no bucket configured, services/cloud_storage.py
# reports "not enabled" and everything writes to the local disk as before.
boto3==1.34.162

# ---- Production WSGI ----
gunicorn==22.0.0
